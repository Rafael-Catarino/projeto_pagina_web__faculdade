const btnIndex = document.querySelectorAll("button");

for (let i = 0; i < btnIndex.length; i++) {
  btnIndex[i].addEventListener("click", () => {
    alert("Ainda está em desenvolvimento.");
  });
}
